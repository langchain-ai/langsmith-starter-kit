LANGSMITH_TRACING=true
LANGSMITH_ENDPOINT="https://api.smith.langchain.com"
LANGSMITH_API_KEY="<redacted>"
LANGSMITH_WORKSPACE_ID="<redacted>" # Optional, only specify if your API key has access to multiple workspaces
# Project names are set automatically per use case:
#   email-agent → starter-kit-email-agent
#   chatbot     → starter-kit-chatbot
OPENAI_API_KEY="<redacted>"
